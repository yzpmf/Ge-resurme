# 个人网站 开发文档

> 用途：每次开发前先读这份文档，了解项目现状、约定和下一步，读完即可接着开发。
> 最后更新：2026-06-16

---

## 1. 项目概览

- **是什么**：个人主页 / 简历站，纯静态（HTML + CSS + 原生 JS，无构建步骤、无框架）。
- **域名**：gezhenghao.com
- **源码目录**：`D:\things for q claw\web`
- **GitHub 仓库**：https://github.com/yzpmf/Ge-resurme
- **托管**：**GitHub + Netlify 静态托管**（push 到 GitHub，Netlify 自动部署）。
- **主题**：天蓝 + 淡黄，配色全部走 `css/style.css` 顶部的 `:root` CSS 变量。

### 目录结构
```
web/
├─ index.html            # 单页主站（导航/Hero/经历/技能/项目/文章/联系/页脚）
├─ css/style.css         # 全站样式（含 :root 变量、留言表单样式）
├─ js/
│  ├─ main.js            # 导航、滚动动画(fade-in)、交互
│  ├─ articles.js        # 文章/博客列表渲染
│  └─ contact.js         # ★留言表单逻辑（两套方案切换开关在文件顶部）
├─ articles/             # Markdown 文章
├─ images/               # 图片（含微信公众号二维码、项目图）
├─ project/              # 子项目页（标准偏差计算器 / 角加速度 / yvshanfang）
└─ server/               # ★联系表单“服务器方案”后端（静态方案用不到）
   ├─ contact-server.js
   ├─ package.json
   ├─ .env.example
   └─ .gitignore
```

---

## 2. 当前进度（开发到哪了）

### ✅ 已完成
- 主站全部静态区块：导航、Hero、学习经历(timeline)、技能、项目、文章博客、联系方式、页脚。
- **联系区留言功能（本次新增，两套方案均已落地代码）**：
  - `index.html` `#contact` 内新增 `<form id="contactForm" class="contact-form">`，含 称呼 / 邮箱 / 留言 三个字段 + 蜜罐反垃圾(`bot-field`)。
  - `css/style.css` 新增留言表单样式（配套主题，含移动端单列、聚焦高亮、状态提示色）。
  - `js/contact.js` 统一处理提交，**顶部 `CONFIG.mode` 一键切换两套方案**。
  - `server/` 后端（服务器方案用）已写好，可直接 `npm install && npm start`。
- 联系区原有的 邮箱/GitHub/知乎/公众号二维码 直接联系方式保留。

### ⏳ 待办 / 下一步（TODO）
- [ ] **二选一并真正启用**（见第 3 节，目前默认 `netlify` 模式）：
  - 静态方案：在 Netlify 后台加“邮件通知”，把留言转发到指定邮箱。
  - 服务器方案：部署 `server/`，配好 SMTP，前端切到 `server` 模式。
- [ ] 留言成功后可加更友好的提示动画（目前是文字状态）。
- [ ] 可选：给表单加简单频率限制 / 验证码，进一步防刷（静态方案靠蜜罐 + Netlify 自带垃圾过滤；服务器方案可在后端按 IP 限流）。
- [ ] 收信邮箱最终定哪个还没定（学校邮箱 yzpmf@swjtu.edu.cn / 个人邮箱，按需在通知里设）。

---

## 3. 留言功能：两套方案

两套方案**共用同一段表单 HTML**，区别只在“留言提交后由谁负责发邮件”。切换点在 `js/contact.js` 顶部：

```js
var CONFIG = {
  mode: 'netlify',                 // 'netlify' | 'server'
  serverEndpoint: 'https://your-server.example.com/api/contact'
};
```

### 方案 A —— 静态托管方案（Netlify Forms）【当前默认】
- **原理**：表单带 `data-netlify="true"` + 隐藏 `form-name`，Netlify 部署时自动识别；`contact.js` 在 netlify 模式下以 AJAX 把数据 POST 到站点根路径 `/`，Netlify 接住并存进后台。
- **启用步骤**：
  1. 确认 `mode: 'netlify'`（默认）。
  2. push 部署到 Netlify。
  3. Netlify 后台 → 站点 → **Forms** 里能看到 `contact` 表单提交记录。
  4. **Site settings → Forms → Form notifications → Add notification → Email notification**，填要收留言的邮箱 → 以后每条留言自动转发到邮箱。
- **额度**：免费版 100 条/月。
- **优点**：零后端、零运维。**缺点**：绑定 Netlify；额度有限。

### 方案 B —— 服务器方案（自建后端 + SMTP）
- **原理**：`contact.js` 在 server 模式下把留言以 JSON `POST` 到 `serverEndpoint`；后端 `server/contact-server.js`（Node 内置 http + nodemailer）用 SMTP 把留言发到邮箱。
- **启用步骤**：
  1. `cd server && npm install`
  2. `cp .env.example .env`，填好 `SMTP_HOST/PORT/USER/PASS`（PASS 是邮箱**授权码**不是登录密码）、`MAIL_TO`、`ALLOW_ORIGIN=https://gezhenghao.com`。
  3. `npm start`（= `node --env-file=.env contact-server.js`，需 Node ≥ 20.6；旧版本改用环境变量方式启动）。
  4. 把后端跑在有公网/可被站点访问的地址，建议套 HTTPS 反代（如 `https://api.gezhenghao.com/api/contact`）。
  5. `js/contact.js` 改 `mode: 'server'`，`serverEndpoint` 填该地址，push 部署。
- **接口**：`POST /api/contact`，body `{name,email,message}`；另有 `GET /health` 健康检查。
- **优点**：完全自主、可扩展（存库、限流、多通知）。**缺点**：要自己部署运维、保证可用性 + HTTPS + CORS。
- **安全提醒**：生产务必把 `ALLOW_ORIGIN` 设成自己的域名；`.env` 已在 `.gitignore`，**别提交**。

> 也可换第三方表单后端（Formspree / Web3Forms / Getform）——属于方案 A 的变体，把表单 action 指到它们即可，原理一样。

---

## 4. 部署 / Git 约定

- 改完 push 到 `yzpmf/Ge-resurme`，Netlify 自动部署。
- **git 代理坑**：全局配了代理 `http://127.0.0.1:7897`(Clash)，代理没开时 push 会报 `Failed to connect to 127.0.0.1 port 7897`。这不是代码问题，用直连绕过：
  ```
  git -c http.proxy= -c https.proxy= push ...
  ```
  凭据用 GCM（已存 token，无需重新登录）。

---

## 5. 本地预览

纯静态，直接浏览器打开 `index.html` 即可看大部分效果。注意：
- **Netlify 表单 AJAX 提交（方案 A）在本地打开时不会真正成功**（没有 Netlify 接口），需部署后测；本地能验证 UI / 校验 / 蜜罐逻辑。
- 方案 B 可本地起 `server/` 后，把 `serverEndpoint` 指到 `http://localhost:3001/api/contact` 联调。
