# 个人网站 开发文档

> 用途：每次开发前先读这份文档，了解项目现状、约定和下一步，读完即可接着开发。
> 最后更新：2026-06-16

---

## 1. 项目概览

- **是什么**：个人主页 / 简历站，纯静态（HTML + CSS + 原生 JS，无构建步骤、无框架）。
- **域名**：gezhenghao.com（Netlify 项目名 `elaborate-crostata-abf616`）。
- **源码目录**：`D:\things for q claw\web`
- **GitHub 仓库**：https://github.com/yzpmf/Ge-resurme （默认分支 `master`）。
- **托管**：**GitHub + Netlify 静态托管**（push 到 `master`，Netlify 自动部署发布）。
- **主题**：天蓝 + 淡黄，配色全部走 `css/style.css` 顶部的 `:root` CSS 变量。

### 目录结构
```
web/
├─ index.html            # 单页主站（导航/Hero/经历/技能/项目/文章/联系/页脚）
├─ css/style.css         # 全站样式（含 :root 变量、留言表单样式）
├─ js/
│  ├─ main.js            # 导航、滚动动画(fade-in)、交互
│  ├─ articles.js        # 文章/博客列表渲染
│  └─ contact.js         # ★留言表单逻辑（顶部 CONFIG 一键切换三套发信方案）
├─ articles/             # Markdown 文章
├─ images/               # 图片（含微信公众号二维码、项目图）
├─ project/              # 子项目页（标准偏差计算器 / 角加速度 / yvshanfang）
└─ server/               # ★联系表单“服务器方案”后端（其它方案用不到）
   ├─ contact-server.js
   ├─ package.json
   ├─ .env.example
   ├─ .env               # 真实 SMTP 配置，已 gitignore，不提交
   └─ .gitignore
```

### 相关邮箱
- **收留言/通知**：QQ `2298209797@qq.com`（QQ 实测收不到 Netlify 通知，正改用下面学校邮箱）。
- **学校教学邮箱**：`yzpmf@my.swjtu.edu.cn`（当前要把留言改投到这里）。
- **服务器方案发信账号**：Outlook `gzh.zh@outlook.com`（配置在 `server/.env`，凭据另见 Claude 长期记忆 `email-accounts`）。

---

## 2. 当前进度（开发到哪了）

### ✅ 已完成
- 主站全部静态区块：导航、Hero、学习经历(timeline)、技能、项目、文章博客、联系方式、页脚。
- **联系区留言功能**（三套发信方案均已落地代码，靠 `contact.js` 顶部 `CONFIG.mode` 切换）：
  - `index.html` `#contact` 内 `<form id="contactForm" class="contact-form">`：称呼/邮箱/留言 + 蜜罐(`bot-field`)。
  - `css/style.css` 配套主题样式（移动端单列、聚焦高亮、状态提示色）。
  - `js/contact.js` 统一提交逻辑，支持 `netlify` / `web3forms` / `server` 三种模式。
  - `server/` 后端（服务器方案）已写好，`npm install` 完成、`nodemailer` 已装。
- **Netlify 表单检测已开启**：原先表单提交 `POST / → 404`，根因是 Netlify「表单检测」默认**关闭**。已在后台「形式(Forms)→启用表单检测」并「清除缓存并部署」后，**提交成功、留言进 Netlify 后台**。

### 🔴 当前卡点（正在解决）
- **留言能进 Netlify 后台，但邮件收不到**：QQ 没收到 Netlify 的通知邮件（Netlify 免费通知从其境外域名发信，常被 QQ/学校邮箱拦截/丢垃圾箱）。
- **结论**：放弃依赖 Netlify 后台通知，**改用 Web3Forms**（投递更稳、纯代码控制、绕开本地 Clash）。代码已就绪（`contact.js` 已加 `web3forms` 模式），**就差一个 access key**。

### ⏳ 待办 / 下一步（TODO）
- [ ] **【进行中】启用 Web3Forms**：用户去 web3forms.com 用 `yzpmf@my.swjtu.edu.cn` 注册拿 access key（同时验证学校邮箱能否收外部邮件）→ 填进 `contact.js` 的 `CONFIG.web3formsKey` → `mode` 改 `'web3forms'` → push。
- [ ] 若学校邮箱连 Web3Forms 的 key 邮件都收不到 → 改用 QQ 注册 key 再试。
- [ ] 服务器方案待用户那台服务器配好后再启用（见第 3 节方案 C）；注意本地因 Clash 无法测 SMTP（见第 5 节坑）。
- [ ] 留言成功后可加更友好的提示动画（目前是文字状态）。
- [ ] 可选：表单加频率限制/验证码进一步防刷。

---

## 3. 留言功能：三套发信方案

三套方案**共用同一段表单 HTML**，区别只在“留言由谁负责发邮件”。切换点在 `js/contact.js` 顶部：

```js
var CONFIG = {
  mode: 'netlify',              // 'netlify' | 'web3forms' | 'server'
  serverEndpoint: 'https://your-server.example.com/api/contact', // server 模式用
  web3formsKey: 'PASTE_YOUR_ACCESS_KEY'                          // web3forms 模式用
};
```

### 方案 A —— Netlify Forms（纯静态，当前线上模式）
- **原理**：表单带 `data-netlify="true"` + 隐藏 `form-name`；`netlify` 模式下 `contact.js` 以 AJAX 把表单数据 POST 到站点根 `/`，Netlify 接住存进后台。
- **前置坑**：Netlify「表单检测」**默认关闭**，不开就 `POST / → 404`。开启后**必须重新部署**（清缓存部署）才会扫描注册表单。
- **要收到邮件**：还需在后台配「表单提交通知 → Email notification」。**但实测 QQ 收不到**（投递被拦），所以本项目改走方案 B。
- **优点**：零代码后端。**缺点**：依赖 Netlify 后台配置（无法用代码改收件人）、免费通知投递不稳、100 条/月。

### 方案 B —— Web3Forms（纯静态 + 第三方投递，**推荐/目标方案**）
- **原理**：`web3forms` 模式下 `contact.js` 把留言 JSON POST 到 `https://api.web3forms.com/submit`，带 `access_key`；邮件由 Web3Forms 服务器投递到注册该 key 时用的邮箱。
- **启用步骤**：
  1. 去 https://web3forms.com 用目标收件邮箱（`yzpmf@my.swjtu.edu.cn`）创建 access key，邮箱会收到 key。
  2. 把 key 填进 `CONFIG.web3formsKey`，`mode` 改 `'web3forms'`。
  3. push 部署。
- **注意**：Web3Forms 即使出错也返回 HTTP 200，`contact.js` 已据返回体 `success` 字段判定成败。access key 是**公开安全**的（只能提交、读不了数据），可放前端/提交 git。
- **优点**：纯代码控制收件人、投递稳、绕开本地 Clash、不碰任何后台。**缺点**：留言经过第三方一道。

### 方案 C —— 自建服务器 + SMTP
- **原理**：`server` 模式下 `contact.js` 把留言 JSON POST 到 `serverEndpoint`；后端 `server/contact-server.js`（Node 内置 http + nodemailer）用 SMTP 发邮件。
- **启用步骤**：
  1. `cd server && npm install`（已装）。
  2. `cp .env.example .env`，填 `SMTP_*`（密码填**授权码**）、`MAIL_TO`、`ALLOW_ORIGIN=https://gezhenghao.com`。当前 `.env` 已配 Outlook 发信。
  3. `npm start`（= `node --env-file=.env contact-server.js`，需 Node ≥ 20.6）。
  4. 后端跑在有公网且套 HTTPS 的地址（如 `https://api.gezhenghao.com/api/contact`）。
  5. `contact.js` 改 `mode: 'server'`、填 `serverEndpoint`，push。
- **接口**：`POST /api/contact` body `{name,email,message}`；`GET /health` 健康检查；已带 CORS、蜜罐、邮箱格式校验。
- **优点**：完全自主、可扩展。**缺点**：要自己部署运维 + HTTPS + CORS。
- **安全**：生产把 `ALLOW_ORIGIN` 设成自己域名；`.env` 已 gitignore，别提交。

---

## 4. 部署 / Git 约定

- 改完 push 到 `yzpmf/Ge-resurme` 的 `master`，Netlify 自动部署。
- **git 代理坑**：全局配了代理 `http://127.0.0.1:7897`(Clash)，代理没开时 push 报 `Failed to connect to 127.0.0.1 port 7897`。用直连绕过：
  ```
  git -c http.proxy= -c https.proxy= push ...
  ```
  凭据用 GCM（已存 token，无需重新登录）。
- **Netlify 重新部署/扫描表单**：后台 → 部署(Deploys) → 触发释放(Trigger deploy) → **部署项目时不带缓存**（Clear cache and deploy）。

---

## 5. 本地预览 / 测试

纯静态，浏览器直接开 `index.html` 可看大部分效果。注意：
- **方案 A(Netlify) 的 AJAX 提交本地打不通**（无 Netlify 接口），需部署后测；本地只能验证 UI/校验/蜜罐。
- **方案 C 本地测 SMTP 会失败 —— Clash 的坑**：用户在国内必须挂 Clash 才能上网，而 Clash 开 TUN/fake-ip 时会把 `smtp-mail.outlook.com`、`smtp.qq.com` 等 DNS 劫持成 `198.18.x.x` 虚拟 IP，SMTP 连接被代理掐断（`Connection established → Connection closed`）。**本机关代理又上不了网**，所以本地无法验证 SMTP 凭据；要测就到那台干净的服务器上测，或直接靠方案 B 绕开。
- 方案 C 本地联调（若网络允许）：起 `server/` 后把 `serverEndpoint` 指向 `http://localhost:3001/api/contact`。
- `server/test-send.js` 是临时 SMTP 测试脚本（验证登录+发一封测试信），未提交，可保留复用。
